import Layout from "../layouts";
import { Input } from "@geist-ui/core";

const Home = () => {
  return (
    <Layout>
      <Input placeholder="Search movies & music"/>
    </Layout>
  );
};

export default Home;
